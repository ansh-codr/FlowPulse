# FlowPulse Dashboard

React + Vite + Tailwind dashboard that reads Supabase data, subscribes to realtime events, and renders focus analytics.

## Setup

```bash
cd dashboard
npm install
cp ../.env.example .env && # fill VITE_ values
npm run dev
```

Deploy to Netlify using `npm run build` and set environment variables in the Netlify UI.
